name: Crypto Market Pipeline

on:
  workflow_dispatch:

  schedule:
    - cron: "*/15 * * * *"

jobs:
  run-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test PostgreSQL connection
        env:
          DATABASE_HOST: ${{ secrets.DATABASE_HOST }}
          DATABASE_PORT: ${{ secrets.DATABASE_PORT }}
          DATABASE_NAME: ${{ secrets.DATABASE_NAME }}
          DATABASE_USER: ${{ secrets.DATABASE_USER }}
          DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
        run: |
          python -c "from scripts.db_connection import get_postgres_engine; engine=get_postgres_engine(); connection=engine.connect(); print('SUCCESS: PostgreSQL connection works from GitHub Actions'); connection.close(); engine.dispose()"